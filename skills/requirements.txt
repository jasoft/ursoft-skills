# Minimal dependencies - uses system tools and localmac-ai-ocr
# Python standard library only (subprocess, json, time, pathlib, etc.)
# Ensure localmac-ai-ocr is installed and its Python venv is available
