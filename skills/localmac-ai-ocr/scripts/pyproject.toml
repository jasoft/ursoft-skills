[project]
name = "localmac-ai-ocr"
version = "0.1.0"
description = "Local macOS GUI capture and OCR helpers"
requires-python = ">=3.11"
dependencies = [
  "Pillow>=10.0.0",
  "requests>=2.31.0",
]

[tool.uv]
package = false
